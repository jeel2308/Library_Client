const buttonGroupStyle = {
  display: 'flex',
  justifyContent: 'space-between',
};

const linkStyle = {
  display: 'flex',
};

const iconStyle = {
  size: 28,
};

export { buttonGroupStyle, linkStyle, iconStyle };
